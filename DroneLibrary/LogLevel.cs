namespace DroneLibrary
{
    /// <summary>
    /// Gibt die verschiedene Level für die Einträge an.
    /// </summary>
    public enum LogLevel
    {
        Verbose, 
        Debug, 
        Info, 
        Warning, 
        Error
    }
}
